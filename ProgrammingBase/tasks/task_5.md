# Домашняя работа 5 - Функции (дедлайн 15.11.25 включительно, до 15 баллов)

## Критерии оценивания

| Критерий                        | 3 балла                                                                                                                                                                                 | 1 балл                                                                                    | 0 баллов                                                                                                  |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| **Каждая функция** | Полностью соответствует заданию: корректные входы/выходы, логика верна, покрыты граничные случаи                                                                                        | Небольшие недочёты: частичные ошибки, не все края учтены, но результат в целом верный     | Существенные ошибки, неверные типы/контракты, отсутствует или не работает                                 |
| Общая реализация                | Чистая архитектура (разделение ответственности, переиспользование кода), корректный ввод/вывод, формат отчёта как в условии, обработка пустого ввода/ошибок, стиль кода единообразен | В целом ок, но есть мелкие огрехи (формат, слабая обработка ошибок, дублирование кода) | Хаотичная структура, нарушение формата, отсутствие обработки ошибок, логика «в main», игнор требований |




**Штрафы (за каждую задачу)**
- незначительные ошибки в алгоритме (граничные условия, отдельные случаи) - **0.5 балла**
- неверный формат ввода/вывода - **0.5 балла**
- значительные ошибки в алгоритме (неверно обрабатывается целый класс входных данных) - **1.5 балла**
- критические ошибки в алгоритме (неверно обрабатывается несколько классов входных данных) - **все баллы**

**Штрафы (за общее решение)**
- незначительные огрехи стиля (число пробелов в отступе, неудачно расставленные скобки) - **10% с округлением до 0.5 в большую сторону**
- значительные огрехи стиля (имена переменных в стиле `a`, `b`, `c`, строки кода, не умещающиеся в экран) - **30% с округлением до 0.5 в большую сторону**

---

<details>
<summary><strong style="font-size: 18px;">Вариант 1</strong></summary>

### Тема: Анализ оценок студентов

#### Цель

Разработать программу для анализа списка оценок студентов и формирования отчёта о результатах группы.

---

### Условие задачи

Напишите программу, которая получает на вход список оценок студентов (целые числа от 1 до 5) и выполняет несколько шагов анализа, реализованных через **4 функции**:

1. **`read_grades()`**
   Принимает строку от пользователя (например: `"5 3 4 5 2 5 4 3"`)
   Возвращает список оценок в виде чисел.
   Если ввод содержит недопустимые значения (например, не числа или оценки вне диапазона 1–5), функция должна игнорировать их.

   **Пример:**

   ```python
   >>> read_grades("5 3 4 x 7 2")
   [5, 3, 4, 2]
   ```

---

2. **`average_grade(grades)`**
   Принимает список оценок.
   Возвращает среднюю оценку (с плавающей точкой, округлённую до двух знаков).
   Если список пуст, возвращает `0`.

   **Пример:**

   ```python
   >>> average_grade([5, 4, 3])
   4.0
   ```

---

3. **`grade_distribution(grades)`**
   Принимает список оценок.
   Возвращает словарь, где ключ — оценка (от 1 до 5),
   а значение — количество студентов, получивших её.
   Если какая-то оценка не встречается, её можно не включать в словарь.

   **Пример:**

   ```python
   >>> grade_distribution([5, 4, 3, 5])
   {5: 2, 4: 1, 3: 1}
   ```

---

4. **`print_report(grades)`**
   Принимает список оценок.
   Использует функции `average_grade()` и `grade_distribution()` для вывода итогового отчёта.

   **Выводит:**

   ```
   Средний балл: 4.25
   Распределение оценок:
     5: 3 студентов
     4: 2 студентов
     3: 1 студент
   ```

---

### 🧠 Пример работы программы

```python
def main():
    data = input("Введите оценки через пробел: ")
    grades = read_grades(data)
    print_report(grades)

if __name__ == "__main__":
    main()
```

**Пример ввода и вывода:**

```
Введите оценки через пробел: 5 4 5 3 5 2 4
Средний балл: 4.00
Распределение оценок:
  5: 3 студентов
  4: 2 студентов
  3: 1 студент
  2: 1 студент
```

</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 2</strong></summary>

### Тема: Анализ личных расходов

#### Цель

Разработать программу, которая помогает пользователю проанализировать свои расходы за месяц.

---

### Условие задачи

Пользователь вводит список покупок в виде строк, где каждая запись содержит категорию и сумму через запятую.
Например:

```
еда, 1200
транспорт, 350
еда, 800
развлечения, 500
```

Необходимо реализовать **4 функции**, которые выполняют анализ и выводят отчёт.

---

### 1. `read_expenses()`

Принимает ввод пользователя построчно (до пустой строки).
Каждая строка содержит категорию и сумму через запятую.
Возвращает список кортежей вида `(категория: str, сумма: float)`.

**Требования:**

* Игнорировать строки с ошибками формата.
* Пропускать отрицательные или нулевые суммы.

**Пример:**

```python
>>> read_expenses()
еда, 1200
транспорт, 350
еда, 800
развлечения, 500

[('еда', 1200.0), ('транспорт', 350.0), ('еда', 800.0), ('развлечения', 500.0)]
```

---

### 2. `total_by_category(expenses)`

Принимает список кортежей `(категория, сумма)`.
Возвращает словарь, где ключ — категория, значение — общая сумма расходов по ней.

**Пример:**

```python
>>> total_by_category([('еда', 1200.0), ('транспорт', 350.0), ('еда', 800.0)])
{'еда': 2000.0, 'транспорт': 350.0}
```

---

### 3. `biggest_expense(expenses)`

Принимает список кортежей `(категория, сумма)`.
Возвращает кортеж с категорией, в которой был **самый крупный единичный расход**.
Если расходов нет, возвращает `None`.

**Пример:**

```python
>>> biggest_expense([('еда', 1200.0), ('транспорт', 350.0), ('еда', 800.0)])
('еда', 1200.0)
```

---

### 4. `print_report(expenses)`

Выводит итоговый отчёт, используя результаты функций `total_by_category()` и `biggest_expense()`.

**Пример вывода:**

```
--- Отчёт о расходах ---
Всего категорий: 3
Суммы по категориям:
  еда — 2000.00 ₽
  транспорт — 350.00 ₽
  развлечения — 500.00 ₽
Самая крупная трата: еда — 1200.00 ₽
```

---

### 🧠 Пример работы программы

```python
def main():
    print("Введите расходы (категория, сумма). Пустая строка — конец ввода.")
    expenses = read_expenses()
    print_report(expenses)

if __name__ == "__main__":
    main()
```

</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 3</strong></summary>

## Тема: Текстовый анализ мини-статьи

### Цель

Написать программу, которая нормализует текст, отфильтровывает “стоп-слова”, считает частоты слов и формирует краткий отчёт.

## Условие задачи

Пользователь вводит произвольный текст из нескольких строк. Необходимо реализовать 4 функции.

1. `normalize(text: str) -> list[str]`
   Приводит текст к нижнему регистру, удаляет пунктуацию (кроме дефиса внутри слова), разбивает на токены по пробелам.
   Требования:

   * подряд идущие небуквенные символы считать разделителями;
   * числа и слова смешанного вида оставлять как токены (например, `web3` допустимо);
   * пустые токены не возвращать.

   Пример:

   ```python
   >>> normalize("Привет, мир! Это — тест-текст.")
   ["привет", "мир", "это", "тест-текст"]
   ```

2. `filter_stopwords(tokens: list[str], stopwords: set[str]) -> list[str]`
   Удаляет из списка токенов все слова, входящие в множество `stopwords`, а также токены длиной 1.
   Пример:

   ```python
   >>> filter_stopwords(["это","тест","текст"], {"это","и","на"})
   ["тест","текст"]
   ```

3. `word_frequencies(tokens: list[str]) -> dict[str, int]`
   Считает частоты оставшихся слов.
   Пример:

   ```python
   >>> word_frequencies(["тест","текст","тест"])
   {"тест": 2, "текст": 1}
   ```

4. `print_report(text: str, stopwords: set[str], top_n: int = 5) -> None`
   Строит отчёт, используя функции 1–3. В отчёт вывести:

   * общее число токенов до фильтрации;
   * число токенов после фильтрации;
   * количество уникальных слов;
   * число “hapax” — слов, встречающихся ровно один раз;
   * среднюю длину слова (после фильтрации), округлённую до двух знаков;
   * топ-`N` слов по убыванию частоты (при равенстве — по алфавиту).

   Формат примера вывода:

   ```
   Всего токенов (до фильтрации): 18
   После фильтрации: 12
   Уникальных слов: 7
   Hapax (встретились 1 раз): 4
   Средняя длина слова: 5.33
   Топ-5 слов:
     1) данные — 3
     2) анализ — 2
     3) модель — 2
     4) вывод — 2
     5) текст — 1
   ```

## Пример запуска

```python
def main():
    print("Введите текст (пустая строка — конец ввода):")
    lines = []
    while True:
        line = input()
        if not line:
            break
        lines.append(line)
    text = "\n".join(lines)

    # минимальный набор стоп-слов (можете расширить)
    stop = {"и", "а", "но", "на", "в", "к", "по", "с", "из", "что", "это"}

    print_report(text, stop, top_n=5)

if __name__ == "__main__":
    main()
```
</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 4</strong></summary>

## Тема: Планировщик задач на день

### Цель

Написать программу, которая принимает список задач с приоритетами и оценкой длительности, строит сводку и формирует план на фиксированное время рабочего дня.

## Условие задачи

Пользователь вводит строки вида
`название; приоритет; минуты`

Где:

* `приоритет` — целое 1..3 (1 — низкий, 2 — средний, 3 — высокий).
* `минуты` — целое положительное число (оценка длительности).

Пример ввода:

```
Проверить почту; 1; 10
Написать отчёт; 3; 90
Звонок клиенту; 2; 30
Код-ревью; 2; 45
```

Нужно реализовать 4 функции.

1. `read_tasks() -> list[tuple[str, int, int]]`
   Считывает строки до пустой строки.
   Возвращает список кортежей `(title, priority, minutes)`.
   Требования:

   * Игнорировать строки, где не удаётся распарсить три поля.
   * Отбрасывать задачи с приоритетом вне 1..3 и с `minutes <= 0`.
   * Обрезать пробелы у полей; `title` не должен быть пустым.

2. `summarize(tasks: list[tuple[str, int, int]]) -> dict`
   Строит сводку по задачам. Возвращает словарь с ключами:

   * `"count"` — общее число задач,
   * `"total_minutes"` — суммарная длительность,
   * `"by_priority"` — словарь `{1: (count, minutes), 2: ..., 3: ...}` (ключ присутствует только если есть задачи такого приоритета).

3. `plan_day(tasks: list[tuple[str, int, int]], budget: int) -> tuple[list, list]`
   Формирует план на день при ограничении времени `budget` (в минутах).
   Возвращает кортеж `(selected, deferred)`, где:

   * `selected` — список задач, которые войдут в план,
   * `deferred` — список оставшихся.
     Алгоритм отбора:
   * Сначала сортировать задачи по приоритету по убыванию (3→2→1), при равенстве — по `minutes` по возрастанию, затем по `title` по алфавиту.
   * Идти по отсортированному списку и жадно набирать задачи, пока суммарное время не превысит `budget`.

4. `print_report(tasks: list[tuple[str, int, int]], budget: int) -> None`
   Печатает:

   * сводку из `summarize()`,
   * значение бюджета,
   * список задач, вошедших в план (с накопленным временем),
   * список отложенных задач,
   * коэффициент утилизации времени: `использовано_минут / budget`, округлить до двух знаков.

## Пример запуска

```python
def main():
    print("Введите задачи в формате: название; приоритет(1..3); минуты. Пустая строка — конец.")
    tasks = read_tasks()
    budget = 240  # 4 часа = 240 минут (можно менять)
    print_report(tasks, budget)

if __name__ == "__main__":
    main()
```

### Пример ожидаемого вывода (фрагмент)

```
Всего задач: 4
Суммарное время: 175 мин
По приоритетам:
  3: 1 задач(и), 90 мин
  2: 2 задач(и), 75 мин
  1: 1 задач(и), 10 мин

Бюджет дня: 240 мин
План:
  1) Написать отчёт [prio=3, 90]
  2) Звонок клиенту [prio=2, 30]
  3) Код-ревью [prio=2, 45]
  4) Проверить почту [prio=1, 10]
Итого по плану: 175 мин (утилизация 0.73)

Отложено: нет
```
</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 5</strong></summary>

## Тема: Очистка списка контактов

### Цель

Написать программу, которая принимает «сырые» контакты, нормализует телефоны, проверяет e-mail и формирует отчёт о корректности данных.

## Формат входа

Пользователь вводит несколько строк вида:

```
ФИО; email; телефон
```

Примеры:

```
Иванов Иван; ivan@example.com; +7 (999) 123-45-67
Петрова Анна; anna.pet@domain; 8-999-1234567
Smith John; john.smith@mail.com; 9991234567
; wrong@@mail.com; +7 999 000 00 00
```

## Требуется реализовать 4 функции

1. `parse_contacts() -> list[tuple[str, str, str]]`
   Считывает строки до пустой строки. Возвращает список кортежей `(full_name, email, phone_raw)`.
   Требования:
   — В строке должны быть ровно три поля, разделённые `;`.
   — Лишние пробелы по краям полей обрезать.
   — Пустое ФИО допустимо (оставить пустую строку), остальные проверки — в последующих функциях.
   — Строки с неправильным количеством полей игнорировать.

2. `normalize_phone(phone_raw: str) -> str | None`
   Приводит телефон к формату `+7XXXXXXXXXX` или возвращает `None`, если нормализовать нельзя.
   Правила:
   — Удалить все символы, кроме цифр.
   — Если 11 цифр и первая 8, заменить на +7 и оставить последние 10.
   — Если 11 цифр и первая 7, трактовать как +7 и оставить последние 10.
   — Если ровно 10 цифр, трактовать как российский номер без кода страны → добавить +7.
   — Иначе вернуть `None`.

3. `is_valid_email(email: str) -> bool`
   Возвращает `True`, если e-mail проходит простую проверку формата:
   — Ровно один символ `@`.
   — До `@` есть хотя бы один символ.
   — После `@` есть домен с как минимум одной точкой, у каждой части домена длина ≥ 1 и без пробелов.
   — Недопустимы пробелы и двойные точки `..`.

4. `print_report(contacts: list[tuple[str, str, str]]) -> None`
   Формирует и печатает отчёт:
   — Общее число записей.
   — Число записей с валидным e-mail и валидным телефоном.
   — Список проблемных записей с причинами: «пустое ФИО», «невалидный e-mail», «невалидный телефон».
   — Таблицу нормализованных контактов в виде строк: `ФИО | email | телефон`, где телефон — уже нормализованный (или `—`, если `None`).
   — Детектор дубликатов по e-mail и по телефону: вывести повторы (если один и тот же e-mail/телефон встречается более одного раза).

## Пример каркаса программы

```python
def main():
    print("Введите контакты строками в формате: ФИО; email; телефон. Пустая строка — конец.")
    contacts = parse_contacts()
    print_report(contacts)


if __name__ == "__main__":
    main()
```

</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 6</strong></summary>

## Тема: Анализ погодных данных

#### Цель

Написать программу, которая принимает температурные наблюдения за неделю, вычисляет статистику и формирует краткий отчёт.

---

### Условие задачи

Пользователь вводит 7 строк — температуру (в градусах Цельсия) для каждого дня недели, начиная с понедельника.
Пример ввода:

```
3
5
2
4
1
0
-1
```

Необходимо реализовать **4 функции**, работающих совместно:

1. **`read_temperatures()`**
   Считывает 7 строк (или меньше, если пользователь закончит раньше).
   Возвращает список чисел (float).
   Пропускает некорректные значения (например, текст вместо числа).

2. **`temperature_stats(values: list[float]) -> dict`**
   Возвращает словарь с метриками:

   * `min` — минимальная температура,
   * `max` — максимальная,
   * `avg` — среднее значение (округлённое до 1 знака),
   * `frost_days` — количество дней, где температура < 0.

3. **`detect_trend(values: list[float]) -> str`**
   Анализирует изменения и возвращает:

   * `"warming"` — если значения в целом возрастают;
   * `"cooling"` — если в целом убывают;
   * `"unstable"` — если тенденция не выражена.
     (Оценку можно сделать по знаку разности между последним и первым, с допустимой погрешностью ±1°C.)

4. **`print_report(values: list[float])`**
   Формирует и выводит отчёт, используя функции `temperature_stats()` и `detect_trend()`.

   Пример вывода:

   ```
   --- Отчёт о погоде ---
   Дней с измерениями: 7
   Минимум: -1°C
   Максимум: 5°C
   Средняя: 2.0°C
   Дней с морозом: 1
   Тенденция: warming
   ```

---

### 🧠 Пример каркаса программы

```python
def main():
    print("Введите температуры по дням (7 строк, пустая строка — конец ввода):")
    temps = read_temperatures()
    print_report(temps)


if __name__ == "__main__":
    main()
```
</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 7</strong></summary>

## Тема: Анализ продаж по чекам

### Цель

Принять список строк с позициями чека, посчитать выручку по клиентам, применить скидки “лояльности”, выделить топ клиентов и напечатать отчёт.

## Формат входа

Пользователь вводит строки вида:

```
customer; item; qty; unit_price
```

Примеры:

```
Alice; Latte; 2; 180
Bob; Croissant; 3; 120.5
Alice; Sandwich; 1; 350
```

Требования к данным:

* `qty` — целое число > 0.
* `unit_price` — число > 0.
* Лишние пробелы по краям полей обрезать.
* Строки с неправильным количеством полей или нечисловыми qty/price игнорировать.

## Скидка и НДС

* Скидка “лояльности” 5% на суммарный чек клиента, если его оборот за день > 5000.
* НДС считать 20% от суммы после скидки (только для финальной сводки и по клиентам).

## Требуется реализовать 4 функции

1. `read_checks() -> list[tuple[str, str, int, float]]`
   Считывает строки до пустой строки.
   Возвращает список кортежей `(customer, item, qty, unit_price)` с фильтрацией некорректных строк.

2. `aggregate_sales(rows: list[tuple[str, str, int, float]]) -> dict`
   Строит агрегаты и возвращает словарь со структурой:

   * `"per_customer"`: `{customer: {"items": int, "qty": int, "gross": float}}`,
     где `gross` — сумма без скидки и НДС.
   * `"overall"`: `{"items": int, "qty": int, "gross": float}`.

3. `top_customers(agg: dict, n: int = 3) -> list[tuple[str, float, float]]`
   Возвращает список из максимум `n` клиентов в формате
   `(customer, gross, net_with_vat)`, отсортированный по `gross` по убыванию, при равенстве — по имени по алфавиту.
   Здесь:

   * `discounted = gross * 0.95`, если `gross > 5000`, иначе `gross`;
   * `net_with_vat = discounted * 1.20`.

4. `print_report(rows: list[tuple[str, str, int, float]], n: int = 3) -> None`
   Печатает:

   * общее число позиций, суммарное количество товаров и валовую выручку (gross);
   * число уникальных клиентов;
   * по каждому клиенту: items, qty, gross, discounted, net_with_vat;
   * топ-`n` клиентов по валовой выручке;
   * итог по дню: `overall_discounted` и `overall_net_with_vat` (применяя те же правила скидки отдельно для каждого клиента и суммируя).

## Пример каркаса программы

```python
def main():
    print("Введите позиции чеков строками в формате: customer; item; qty; unit_price. Пустая строка — конец.")
    rows = read_checks()
    print_report(rows, n=3)


if __name__ == "__main__":
    main()
```
</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 8</strong></summary>

## Тема: Анализ логов веб-сервера

### Цель

Принять строки логов в упрощённом формате, посчитать сводные метрики, найти пиковый час и вывести отчёт.

## Формат входа

Каждая строка лога:

```
YYYY-MM-DD HH:MM METHOD PATH STATUS BYTES
```

Примеры:

```
2025-11-08 09:12 GET /api/users 200 532
2025-11-08 09:13 POST /api/login 302 128
2025-11-08 10:01 GET / 200 1024
2025-11-08 10:15 GET /api/users 404 76
```

Требования к данным:

* `METHOD` — строка без пробелов.
* `PATH` — начинается с `/`, без пробелов.
* `STATUS` — целое число.
* `BYTES` — целое число ≥ 0.
* Строки с неверным количеством полей или нечисловыми `STATUS/ BYTES` игнорируются.

## Требуется реализовать 4 функции

1. `read_logs() -> list[tuple[str, str, str, int, int]]`
   Считывает строки до пустой строки. Возвращает список кортежей
   `(ts_minute, method, path, status, bytes_sent)`, где `ts_minute` — строка `"YYYY-MM-DD HH:MM"`.

2. `aggregate(rows: list[tuple[str, str, str, int, int]]) -> dict`
   Строит агрегаты и возвращает словарь со структурой:

   * `"hits"` — общее число запросов,
   * `"bytes_total"` — сумма `BYTES`,
   * `"by_status_class"` — словарь вида `{"2xx": n, "3xx": n, "4xx": n, "5xx": n}`,
   * `"per_hour"` — словарь `{ "YYYY-MM-DD HH": hits }` (часы, без минут),
   * `"per_path"` — словарь `{ path: hits }`.

3. `peak_hour(agg: dict) -> tuple[str, int] | None`
   По полю `per_hour` находит час с максимумом запросов.
   Возвращает кортеж `("YYYY-MM-DD HH", hits)` или `None`, если данных нет.
   При равенстве выбирает лексикографически минимальный час.

4. `print_report(agg: dict, top_n: int = 5) -> None`
   Печатает:

   * общее число запросов и суммарные байты;
   * распределение по классам статусов (2xx/3xx/4xx/5xx);
   * пиковый час с числом запросов;
   * топ-`N` путей по количеству запросов (при равенстве сортировать по алфавиту).

## Пример запуска

```python
def main():
    print("Введите строки логов в формате: YYYY-MM-DD HH:MM METHOD PATH STATUS BYTES. Пустая строка — конец.")
    rows = read_logs()
    agg = aggregate(rows)
    print_report(agg, top_n=5)


if __name__ == "__main__":
    main()
```

## Пример ожидаемого вывода (фрагмент)

```
Всего запросов: 4
Всего передано байт: 1760
Статусы:
  2xx: 2
  3xx: 1
  4xx: 1
  5xx: 0
Пиковый час: 2025-11-08 10 — 2 запрос(а)

Топ-5 путей:
  1) /api/users — 2
  2) / — 1
  3) /api/login — 1
```
</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 9</strong></summary>

## Тема: Проверка расписания встреч на пересечения

### Цель

Принять список встреч за день/неделю, отсортировать их по времени, найти пересечения в одной комнате и в личном расписании, вывести отчёт.

## Формат входа

Каждая строка:

```
title; date; start; duration_min; room; person
```

Где:

* `date` — `YYYY-MM-DD`
* `start` — `HH:MM` (24-часовой)
* `duration_min` — целое > 0
* `room` — строка без `;`
* `person` — имя участника/владельца встречи

Пример:

```
Демо; 2025-11-10; 10:00; 45; A101; Alice
1:1; 2025-11-10; 10:30; 30; A101; Bob
Планирование; 2025-11-10; 11:00; 60; A101; Alice
Стендап; 2025-11-10; 10:15; 15; B202; Alice
```

## Требуется реализовать 4 функции

1. `read_events() -> list[dict]`
   Считывает строки до пустой. Возвращает список словарей с полями:

   * `title` (str)
   * `date` (str `YYYY-MM-DD`)
   * `start` (str `HH:MM`)
   * `duration` (int, минуты)
   * `room` (str)
   * `person` (str)
   * `start_minutes` (int: минуты от 00:00) — вычислить из `start`
   * `end_minutes` (int: `start_minutes + duration`)

   Требования:

   * Игнорировать строки с неверным числом полей или невалидными числами/временем.
   * Обрезать пробелы.
   * Не допускать `duration <= 0`.

2. `events_by_date(evts: list[dict]) -> dict[str, list[dict]]`
   Группирует встречи по дате и сортирует внутри дня по `start_minutes`, затем по `room`, затем по `title`.
   Возвращает словарь `{date: [events...]}`.

3. `find_conflicts(day_events: list[dict]) -> dict`
   На вход — список встреч за один день (уже отсортирован).
   Возвращает словарь с двумя списками конфликтов:

   * `"room_conflicts"` — пересечения в одной комнате,
   * `"person_conflicts"` — пересечения у одного и того же человека.

   Правило пересечения: для пары событий A и B конфликт есть, если
   `A.start_minutes < B.end_minutes` и `B.start_minutes < A.end_minutes`.
   В ответ для каждой конфликтной пары вернуть краткие записи:

   ```
   {"a": titleA, "b": titleB, "room": "A101", "interval": "10:00–10:45 vs 10:30–11:00"}
   ```

   или для персональных:

   ```
   {"a": titleA, "b": titleB, "person": "Alice", "interval": "..."}
   ```

4. `print_report(grouped: dict[str, list[dict]]) -> None`
   Для каждой даты печатает:

   * список встреч (время, комната, человек, название);
   * число встреч;
   * суммарное занятое время в минутах по дню (сумма `duration`);
   * найденные конфликты по комнатам и по персоналиям (или «нет»).

## Пример запуска

```python
def main():
    print("Введите встречи строками в формате:")
    print("title; date(YYYY-MM-DD); start(HH:MM); duration_min; room; person")
    print("Пустая строка — конец.")
    events = read_events()
    grouped = events_by_date(events)
    print_report(grouped)


if __name__ == "__main__":
    main()
```

## Пример ожидаемого вывода (фрагмент)

```
=== 2025-11-10 ===
10:00–10:45 | A101 | Alice | Демо
10:15–10:30 | B202 | Alice | Стендап
10:30–11:00 | A101 | Bob   | 1:1
11:00–12:00 | A101 | Alice | Планирование
Итого встреч: 4; суммарно занято: 150 мин

Конфликты комнат:
  A101: Демо ↔ 1:1 (10:00–10:45 vs 10:30–11:00)

Конфликты персон:
  Alice: Демо ↔ Стендап (10:00–10:45 vs 10:15–10:30)

```
</details>

---

<details>
<summary><strong style="font-size: 18px;">Вариант 10</strong></summary>

## Тема: Проверка CSV с кандидатами

### Цель

Принять «CSV» (с разделителем `;`) с данными кандидатов, проверить записи по правилам и вывести сводку по валидным данным и ошибкам.

## Формат входа

Первая непустая строка — заголовок. Далее строки формата:

```
full_name; email; age; position; salary
```

Пример:

```
full_name; email; age; position; salary
Иван Иванов; ivan@example.com; 27; Backend Engineer; 180000
Анна Петрова; anna@@mail; 22; QA; 90000
; john@corp.com; 35; DevOps; 220000
```

## Правила валидации

* `full_name`: непустая строка.
* `email`: ровно один `@`, без пробелов и `..`, после `@` есть хотя бы одна точка.
* `age`: целое число в диапазоне 14..80.
* `position`: непустая строка.
* `salary`: число ≥ 0.

## Требуется реализовать 4 функции

1. `read_dataset() -> list[dict]`
   Считывает строки до пустой строки.

   * Первая непустая — заголовок: ожидаются поля ровно в порядке `full_name; email; age; position; salary`.
   * Для каждой последующей строки: разбить по `;`, обрезать пробелы, привести `age` к `int`, `salary` к `float` (если нельзя — оставить сырые строки, проверит валидация).
   * Неправильное число полей — строку игнорировать.
     Возвращает список словарей (одинаковая схема ключей).

2. `validate_records(rows: list[dict]) -> tuple[list[dict], list[tuple[dict, list[str]]]]`
   Проверяет каждую запись по правилам и делит на:

   * `valid_rows`: список валидных словарей (с уже приведёнными типами для `age` и `salary`, если удачно).
   * `invalid_rows`: список кортежей `(row, reasons)`, где `reasons` — список строковых причин, например:
     `["empty_full_name", "invalid_email", "age_out_of_range", "bad_salary"]`.

3. `aggregate(valid_rows: list[dict]) -> dict`
   Считает агрегаты по валидным данным и возвращает словарь:

   * `"count"` — число валидных записей,
   * `"avg_age"` — средний возраст (float, округлите при выводе),
   * `"salary_total"` — суммарная зарплата,
   * `"by_position"` — словарь `{position: {"count": n, "salary_total": s, "salary_avg": s/n}}`,
   * `"top_position_by_total"` — кортеж `(position, salary_total)` с максимальным суммарным окладом (при равенстве — лексикографически минимальная должность).

4. `print_report(valid_rows: list[dict], invalid_rows: list[tuple[dict, list[str]]], agg: dict) -> None`
   Печатает:

   * общее число строк, число валидных и невалидных;
   * агрегаты (`avg_age`, `salary_total`, топ-позиция);
   * таблицу по должностям: `position | count | salary_avg | salary_total`;
   * список до 10 проблемных записей с причинами (если их больше — вывести, сколько скрыто).

## Каркас запуска

```python
def main():
    print("Вводите строки CSV с разделителем ';'. Пустая строка — конец ввода.")
    rows = read_dataset()
    valid_rows, invalid_rows = validate_records(rows)
    agg = aggregate(valid_rows)
    print_report(valid_rows, invalid_rows, agg)


if __name__ == "__main__":
    main()
```
</details>

---